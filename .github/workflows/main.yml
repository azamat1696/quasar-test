name: Deploy to Hostinger
on:
  push:
    branches:
      - main # Change this to your main branch name

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 18 # Adjust to your project's Node.js version

      - name: Install Dependencies
        run: npm install

      - name: Build Application
        run: npm run build

      - name: Deploy to Hostinger
        uses: SamKirkland/FTP-Deploy-Action@3.1.1
        with:
          ftp-server: ftp.snonlineshopping.com # Adjust to your Hostinger FTP server
          ftp-username: "u803693674.azamat"
          ftp-password: "AB.aziz412"
          local-dir: dist/spa # Adjust to your build output directory
          git-ftp-args: public_html/test # Adjust to your Hostinger web root directory
